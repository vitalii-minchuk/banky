import { View, Text } from "react-native";
import React, { FC } from "react";

const Payments: FC = () => {
  return (
    <View>
      <Text>Payments</Text>
    </View>
  );
};

export default Payments;
